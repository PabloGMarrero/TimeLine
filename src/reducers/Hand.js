
const initialState = {
    selectCardHandler: (() => { }),
    cancelSelectionHandler: (() => { }),
    showPlacesChoicesHandler: (() => { })
}


export const Hand = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state

    }

}