
const initialState = {
    placeCardHandler: (() => { })
}


export const Board = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state

    }

}